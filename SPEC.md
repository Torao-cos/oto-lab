# おとラボ (Oto Lab) — 音の波形観察＆音源アプリ 仕様書 v1

中学1年理科「音の性質」・高校物理基礎「波・音」の授業用Webアプリ。
教師のプロジェクター投影デモと、生徒の個別操作（Chromebook/スマホ）の両方で使う。

## 全体構成

- **単一ファイル `index.html`**（CSS/JS内包・外部ライブラリなし・外部通信なし・ビルド不要）
- GitHub Pagesで公開予定（HTTPS）
- UIは日本語。ダークテーマ（オシロスコープ風）。波形は明るい緑
- レスポンシブ：PC横長／タブレット／スマホ縦持ちで崩れない。フォント・ボタンは大きめ（投影と小画面の両対応）
- 対象ブラウザ優先順位：①Chrome（Chromebook）②iOS Safari ③Android Chrome
- `<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">`（ピンチ操作をアプリが使うため）
- キャンバス上は `touch-action: none`
- AudioContext はユーザー操作（ボタンタップ）で生成/resumeする（iOS Safariの自動再生制限対策）

## 画面モード

上部にタブ3つ：**[オシロスコープ] [音源] [両方]**

- 「両方」＝上にオシロスコープ、下に音源のコンパクト表示。**音を鳴らしながら波形を観察できる**（うなりの波形を自分の端末1台で見せるための最重要モード）
- スマホ縦持ちでは上下積み。PCでは上下または左右で見やすく

## オシロスコープ機能

### マイク入力
- 「🎤 マイク開始」ボタンで getUserMedia を呼ぶ。constraints は必ず：
  `audio: { echoCancellation: false, noiseSuppression: false, autoGainControl: false }`
  （AGCが効くと「大きい音＝大きい振幅」が観察できなくなるため）
- 権限拒否・未対応時は日本語のエラーメッセージをキャンバス上に表示（「マイクの使用を許可してください」等）
- AnalyserNode：fftSize = 4096、`getFloatTimeDomainData` で波形取得

### 波形描画
- Canvas 2D。グリッド線＋軸ラベル表示：横軸は **ms/div**（1目盛りが何msか明記）、縦軸は相対振幅（±1.0がフルスケール）
- **トリガー機能（既定ON、トグルで切替可）**：バッファ内で「負→正に横切る立ち上がりゼロクロス点」（ヒステリシス付き：-0.01を下回ってから+0.01を上回る点）を探し、そこを左端として描画する。これで周期波形が静止して見える。トリガー点が見つからない場合はバッファ先頭から描画
- 描画は requestAnimationFrame

### 軸スケール
- **既定＝固定スケール**：縦 ±1.0、横 10ms 幅（中学生の声〜1kHz程度が数周期見える）
- 「自動調整」トグル：ONのとき波形の最大振幅・推定周期に合わせて縦横をフィット（変化は緩やかに追従させ、ちらつかせない）
- 「手動」チェックボックス：ONのとき
  - タッチ：横方向ピンチ＝時間軸ズーム、縦方向ピンチ＝振幅軸ズーム
  - マウス：ホイール＝時間軸、Shift＋ホイール＝振幅軸
  - さらに **［時間＋］［時間−］［振幅＋］［振幅−］ボタンを常設**（ピンチが苦手な生徒・教師の確実な操作手段）
- 時間軸レンジ：1ms〜100ms幅、振幅レンジ：±0.05〜±1.0

### 凍結（フリーズ）
- 波形キャンバスをクリック/タップ → その瞬間の波形で停止。もう一度タップ → リアルタイムに戻る
- 停止中は「⏸ 停止中（タップで再開）」と表示
- 停止中も手動ズーム・FFT表示は操作可能（バッファを保持して再描画）

### 振動数表示
- 画面右上に大きく **「441.2 Hz」** のように表示（プロジェクターで教室後方から読めるサイズ）
- 推定方式：**自己相関法（ACF）で基本周波数を推定**。相関ピークが弱い場合はFFTピークにフォールバック
- 無音時（RMS < 0.01）は「--- Hz」
- 更新は約0.5秒ごと、直近数回のメディアンで平滑化（数字が暴れないように）

### FFT表示
- 「FFT」チェックボックス：ONで画面を上下分割し、下半分に周波数スペクトル（0〜5000Hz、リニア軸）を表示
- スペクトルのピーク位置にHz数値を注記
- 倍音構造が見える（矩形波・ノコギリ波の観察用）

## 音源（オシレーター）機能

- **独立した2音源 A / B**。各音源に：
  - ON/OFFトグルボタン（▶ 再生 / ⏸ 停止）
  - 周波数の数値入力（1〜20000 Hz、既定 A=440 / B=444）＋ ▲▼ステッパー（±1Hz）
  - 波形選択：正弦波／矩形波／三角波／ノコギリ波（OscillatorNode の sine/square/triangle/sawtooth）
  - 個別音量スライダー（0〜100、既定50）
- **マスター音量スライダー（既定30%）**＋ **大きな赤い「⏹ 全停止」ボタン**（教室の音事故対策。常に見える位置）
- 再生開始/停止時は GainNode で20msのランプをかける（クリックノイズ防止）
- A・B両方ONのとき：**「うなり：|440 − 444| = 4 Hz」を自動表示**（うなり振動数の学習用）

## フッター

- 使い方の要点（2〜3行）
- 「© SciCos / MIT License」

## 受け入れ基準

1. Chrome最新（PC）でコンソールエラーなし
2. マイク許可なしでも音源機能は完全動作
3. 音源A 440Hz再生→マイクで拾うと周波数表示が 440±5Hz
4. A=440, B=444 同時再生で「うなり 4Hz」表示＋耳でうなりが聞こえる
5. タップで凍結⇔再開が機能
6. 全UIが日本語
